using System;
using System.Collections.Generic;
using System.Text;

namespace Certs
{
    class RequestCert
    {
        public RequestCert()
        {
            //Go get list of folks
            //foreach(var thing in folks)
            //  Console.WriteLine(thing);
            //Console.WriteLine("Would you like to get a cert from any of these folks");
            //var name = Console.ReadLine();
            //var found = folks.FirstOrDefault(c => c.ToLower() == name.ToLower());
            //while(found == null)
            //{
            //Console.WriteLine("Would you like to get a cert from any of these folks");
            //name = Console.ReadLine();
            //found = folks.FirstOrDefault(c => c.ToLower() == name.ToLower());
            //}
            //SendRequest(name);
        }
        private void SendRequest(string name)
        {

        }
    }
}
